language: node_js
node_js:
  - "stable"
before_install: cd backend/
install:
  - npm install
before_script: psql -c 'create database travis;' -U postgres
script:
  - npm test
after_success: npm run coverage
services: postgresql

